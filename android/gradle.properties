# Required because the library depends on AndroidX artifacts (androidx.biometric, androidx.fragment, …).
android.useAndroidX=true
android.enableJetifier=false
# Give the Kotlin/AGP build daemon enough heap.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
